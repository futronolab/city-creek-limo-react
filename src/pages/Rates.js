import { Grid } from '@material-ui/core';

function Rates() {
    return(
        <Grid container spacing={3}>
            <Grid item xs={12}>
                <h1>Rates</h1>
            </Grid>
        </Grid>   
    );
}

export default Rates;